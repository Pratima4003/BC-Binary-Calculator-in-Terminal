# BC-Binary-Calculator-in-Terminal
This is the application of 'bc' command in linux terminal. The bc command executes the arbitrary precision calculator. The same happens when you execute this application.
